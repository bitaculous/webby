---
title: "Home"
seo_title: "Bitaclous"
meta_description: "It's all about the bits, baby!"
meta_keywords: "bitaculous, it's, all, about, the, bits, baby, web, application, development, berlin"
published: true
listed: true
position: 1
---

!!! 5
%html.no-js{ itemscope: true, itemtype: 'http://schema.org/WebPage', lang: '{{ locale }}' }
  {% assign bitaculous = contents.sites.first %}

  {% include head %}

  %body{ id: '{{ page.slug }}', class: 'default canvas' }
    .theatre
      {% block 'theatre' %}

      %header.roof
        {% block 'roof' %}

        .backstage
          .contact
            %h1 Contact

        %nav.channels
          %ul.items
            {% for channel in contents.channels %}

            {% assign type = channel.type | downcase | dasherize %}

            %li{ class: "{{ type }} item" }
              %a{ href: "{{ channel.url }}", title: "{{ channel.description }}", target: '_blank' }
                %i{ class: "icon-{{ type }}" }
            {% endfor %}

        %a.bitaculous{ href: '#', title: "{{ bitaculous.title }} - {{ bitaculous.claim }}" }
          %span.title {{ bitaculous.title }}

          %span.claim {{ bitaculous.claim }}

        {% endblock %}
        -# Block `roof`

      .stage
        {% block 'stage' %}

        %section.outline
          %h1 “We built websites which shine inside and outside.”

        %section.what-we-do
          %h1 “We built websites which shine inside and outside.”

        %section.work
          %h1 “We built websites which shine inside and outside.”

        %section.team
          %h1 “We built websites which shine inside and outside.”

        {% endblock %}
        -# Block `stage`

      %footer.basement
        {% block 'basement' %}

        %h1 Basement

        {% endblock %}
        -# Block `basement`

      {% endblock %}
      -# Block `theatre`

    {{ 'bundle.js' | javascript_tag }}