{% assign type = slide.type | downcase | dasherize %}

{% if slide.type != 'Null' %}
.slide.slick-slide{ class: '{{type}}' }
  .content
    {% if slide.message %}
    %p {{slide.message}}
    {% endif %}

    {% if slide.quote %}
    %h1.title {{slide.quote.title}}

    %p.statement {{slide.quote.statement}}

    %p.author {{slide.quote.author}}
    {% endif %}
{% endif %}