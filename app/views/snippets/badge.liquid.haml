%a.badge{ href: '{%if locale == default_locale %}/{% else %}/{{locale}}{% endif %}', title: '{{bitaculous.title}} - {{bitaculous.claim}}' }
  %span.title {{bitaculous.title}}

  %span.claim {{bitaculous.claim}}